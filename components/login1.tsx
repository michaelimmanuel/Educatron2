import type { NextPage } from "next";
import styles from "./login1.module.css";

const LoginIcon: NextPage = () => {
  return <img className={styles.loginIcon} alt="" src="/login.svg" />;
};

export default LoginIcon;
